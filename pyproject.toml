[project]
name = "fhir-mock-api"
version = "0.1.0"
description = "FHIR pattern API mocked."
readme = "README.md"
authors = [
    { name = "Kaio Custodio" }
]
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.141.1",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.15.0",
    "uvicorn[standard]>=0.52.4",
]

[tool.uv]
package = false
